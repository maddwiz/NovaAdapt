# mobile

Native mobile companion scaffolds.

## iOS Companion

`mobile/ios/NovaAdaptCompanion` contains a SwiftUI scaffold for:
- bridge websocket event feed
- run objective submission
- plan approval/rejection affordances

The scaffold is source-only and intended to be imported into an Xcode project.
